[package]
name = "expansionists"
version = "0.1.0"
edition = "2021"

[dependencies]
godot = "0.3.5"
fastrand = "2.3"

[lib]
crate-type = ["cdylib"]

# cargo build --features visual_debug
[features]
visual_debug = []
